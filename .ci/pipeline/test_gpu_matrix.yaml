# Copyright (c) NVIDIA CORPORATION & AFFILIATES, 2026. ALL RIGHTS RESERVED.
# See file LICENSE for terms.
#
---
job: ucx-test-gpu

failFast: false
timeout_minutes: 160

registry_host: harbor.mellanox.com
registry_auth: ucx_harbor_credentials
registry_path: /ucx/ci

kubernetes:
  cloud: il-ipp-blossom-prod-nbu-swx-ucx
  namespace: nbu-swx-ucx
  limits: "{memory: 8Gi, cpu: 4000m}"
  requests: "{memory: 8Gi, cpu: 4000m}"
  privileged: true

credentials:
  - {credentialsId: 'ucx_harbor_credentials', usernameVariable: 'REPO_USER', passwordVariable: 'REPO_PASS'}


env:
  UCX_REPO: "https://github.com/openucx/ucx.git"
  DOCKER_IMAGE: "harbor.mellanox.com#hpcx/x86_64/ubuntu24.04/builder:doca-2.9.0"
  RUN_TESTS: "yes"
  TEST_PERF: "1"
  ASAN_CHECK: "no"
  VALGRIND_CHECK: "no"
  EXECUTOR_NUMBER: "0"
  RUNNING_IN_AZURE: "no"
  GTEST_MAX_IB_SL: "4"
  nworkers: "4"
  SLURM_NODES: 1
  SLURM_PARTITION: funk
  SLURM_HEAD_NODE: scctl
  SLURM_JOB_TIMEOUT: '02:45:00'
  SCCTL_CREDENTIALS_ID: 'svcnbu-swx-hpcx-scctl'
  JOB_ID_FILE_ROOT: "/mnt/pvc/${JOB_BASE_NAME}"
  TEST_TIMEOUT: 140
  STORAGE_DRIVER: overlay
  CI_IMAGE_TAG: "20260223-1"

empty_volumes:
  - {mountPath: /var/lib/containers/storage, memory: false}

pvc_volumes:
  - {claimName: nbu-swx-ucx-pvc, mountPath: /mnt/pvc, readOnly: false}

runs_on_dockers:
  - {
     file: '.ci/dockerfiles/Dockerfile.build_helper',
     name: 'build_helper_ucx',
     tag: "${CI_IMAGE_TAG}",
     build_args: '--build-arg BASE_IMAGE=dockerhub.nvidia.com/ubuntu:24.04'
    }

matrix:
  axes:
    arch:
      - x86_64
    worker: [0, 1, 2, 3]

taskName: '${arch}/${name}/w${worker}'

steps:
  - name: Allocate Environment
    containerSelector: "{name: 'build_helper_ucx'}"
    parallel: false
    shell: action
    module: slurmCI
    run: allocation
    args:
      partition: "${SLURM_PARTITION}"
      headNode: "${SLURM_HEAD_NODE}"
      nodes: "${SLURM_NODES}"
      jobTimeout: "${SLURM_JOB_TIMEOUT}"
      jobName: "ucx-ci-w${worker}-${BUILD_NUMBER}"
      jobIdFile: "${JOB_ID_FILE_ROOT}/job_id_w${worker}_${BUILD_NUMBER}.txt"
      credentialsId: "${SCCTL_CREDENTIALS_ID}"

  - name: Checkout
    containerSelector: "{name: 'build_helper_ucx'}"
    timeout: "${TEST_TIMEOUT}"
    parallel: false
    shell: action
    module: slurmCI
    run: run
    args:
      jobIdFile: "${JOB_ID_FILE_ROOT}/job_id_w${worker}_${BUILD_NUMBER}.txt"
      testScript: 'git clone "${UCX_REPO}" . && git fetch --no-tags origin "${sha1}" && git checkout ${sha1}'
      headNode: "${SLURM_HEAD_NODE}"
      dockerImage: "${DOCKER_IMAGE}"
      containerName: "ucx-ci-w${worker}-${BUILD_NUMBER}"

  - name: Test
    containerSelector: "{name: 'build_helper_ucx'}"
    timeout: "${TEST_TIMEOUT}"
    parallel: false
    shell: action
    module: slurmCI
    run: run
    args:
      jobIdFile: "${JOB_ID_FILE_ROOT}/job_id_w${worker}_${BUILD_NUMBER}.txt"
      testScript: "./contrib/test_jenkins.sh"
      headNode: "${SLURM_HEAD_NODE}"
      dockerImage: "${DOCKER_IMAGE}"
      containerName: "ucx-ci-w${worker}-${BUILD_NUMBER}"
      slurmEnv: [
        "GTEST_MAX_IB_SL=${GTEST_MAX_IB_SL}",
        "EXECUTOR_NUMBER=${EXECUTOR_NUMBER}",
        "RUNNING_IN_AZURE=${RUNNING_IN_AZURE}",
        "sha1=${sha1}",
        "worker=${worker}",
        "nworkers=${nworkers}",
        "RUN_TESTS=${RUN_TESTS}",
        "TEST_PERF=${TEST_PERF}",
        "ASAN_CHECK=${ASAN_CHECK}",
        "VALGRIND_CHECK=${VALGRIND_CHECK}"
      ]

pipeline_stop:
  containerSelector:
    - "{name: 'build_helper_ucx'}"
  parallel: false
  shell: action
  module: slurmCI
  run: stopAllForBuild
  args:
    credentialsId: "${SCCTL_CREDENTIALS_ID}"
    headNode: "${SLURM_HEAD_NODE}"
    jobIdDir: "${JOB_ID_FILE_ROOT}"
